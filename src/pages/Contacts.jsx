import { useNavigate } from "react-router-dom";
const Contacts = () => {
    const navigate = useNavigate()
    return (
        <>
            {/* <button onClick={()=>navigate("/")} className="border bg-green-500 ml-[10px]">go back</button> */}
        </>
    )
}

export default Contacts;
